<Window x:Class="TemplateProject.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TemplateProject"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="Enter Your Details" FontSize="18" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,0,0,10"/>

        <TextBox Grid.Row="1" Name="InputField1" Width="200" Height="30" Tag="Enter First Value" HorizontalAlignment="Center"/>

        <TextBox Grid.Row="2" Name="InputField2" Width="200" Height="30" 
                 Tag="Enter Second Value" HorizontalAlignment="Center" Margin="0,5,0,5"/>

        <Button Grid.Row="3" Content="Process Input" Width="120" Height="40" 
                Click="ProcessInput_Click" HorizontalAlignment="Center"/>
    </Grid>
    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Background">
                <Setter.Value>
                    <VisualBrush>
                        <VisualBrush.Visual>
                            <TextBlock Text="{Binding Tag, RelativeSource={RelativeSource AncestorType=TextBox}}"
                                       Foreground="Gray" VerticalAlignment="Center" Padding="5"/>
                        </VisualBrush.Visual>
                    </VisualBrush>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
</Window>
